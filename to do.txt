##### TO DO #####

------ SELF FETCHING PLAYLIST NAMES ------
Found a command to get the name of a playlist from its uri:
name = sp.playlist(playlist_URI)["name"]
Could implement this so user only needs to enter a link to a song to add it to playlists.txt. 
Can also change code so it does not need to rely on playlists.txt to provide the name and fetch it from the URI instead. 


----- IMPROVE COPY COMMAND -----
Swap to using robocopy instead of COPY then I can include flags to not copy if the file already exists. 
Ask Dad about the right flags as he has done it for the unity backups batch file. 

------ INSTALL PACKAGES ------
Need to write pip install commands for relevant modules
spotipy, pillow etc.
Need to find which packages 


------ AUTOMATICALLY COPY FILES TO NEW LOCATION? ------
Could write commands to automatically copy folder from current location to another location, e.g., program files?
Not necessary as the shortcut works no matter the location of SpotiShuffler_no_terminal.bat as long as all files are in the same directory. 
But also could be fun so it looks like a proper installer even if it doesnt need to. 


----- AUTOMATICALLY LAUNCH SPOTISHUFFLER AFTER INSTALLER.BAT HAS FINISHED EXECUTING? -----
After installing packages, copying shortcut to to start menu directory and maybe copying SpotiShuffler files to a new location, I could run SpotiShuffler imediately
Find a way to detect user input to ask user if they want to run after finishing installing?



##### COMPLETED ITEMS #####

----- COPY SHORTCUT TO START MENU FOLDER ----- 
- need to copy SpotiShuffler.lnk to C:\Users\samue\AppData\Roaming\Microsoft\Windows\Start Menu so that it can be detected as an app and therefore added to the start menu. 
Could be done through: 
cp ./SpotiShuffler.lnk %AppData%/Microsoft\Windows\"Start Menu"
Need to test this. 
installer.bat needs to run with admin privilidges to access %AppData%

@@@@@ SOLUTION @@@@@
command:
COPY ".\SpotiShuffler.lnk" "%AppData%\Microsoft\Windows\Start Menu"
copies the file to the right locationa and appears to be able to  be executed without needing admin privilidges. 
